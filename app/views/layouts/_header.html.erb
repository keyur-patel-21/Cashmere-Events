<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <%= link_to "CASHMERE", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Free Events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Today's Events</a>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "List Your Event", new_event_path, class:"btn btn-dark border border-white" %>
          <% end %>
        </li>
      </ul>
      <div class="d-flex">
        <% if user_signed_in? %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class:"btn btn-dark border border-white"%>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class:"btn btn-dark border border-white" %>
          <%= link_to "Sign Up", new_user_registration_path, class:"btn btn-dark border border-white" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
